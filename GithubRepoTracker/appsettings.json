{

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "ApiBaseUrl": "Your Base URL",
  "ApiEndpoints": {
    "GetAllReposEndpoint": "Endpoint that gets all the repos",
    "GetAllTopicsEndpoint": "Endpoint that gets all the topics",
    "GetAllLanguagesEndpoint": "Endpoint that gets all the languages",
    "GetAccessToken": "Endpoint that gets the bearer token"
  },
  "AllowedHosts": "*",
  "AuthCredentians": {
    "UserName": "Your App's user name",
    "apiPassword": "Your app's password'"
  }




}
